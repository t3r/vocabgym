[pytest]
# Hard per-test timeout so a stuck moto/boto/network call fails fast instead of
# hanging the whole run. 'thread' method interrupts even blocking C/network calls.
timeout = 60
timeout_method = thread
testpaths = tests
